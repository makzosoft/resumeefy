import type { Metadata } from "next";
export const metadata: Metadata = { title: "Career Courses", description: "Study practical, role focused career courses with quizzes, projects and readiness milestones.", alternates: { canonical: "/courses" } };
export default function Layout({children}:{children:React.ReactNode}){return children;}
