import type { Metadata } from "next";
export const metadata: Metadata = { title: "Resume Builder", description: "Build a role tailored, ATS friendly resume with Resumeefy and prepare it for the opportunity you want.", alternates: { canonical: "/resume-builder" } };
export default function Layout({children}:{children:React.ReactNode}){return children;}
