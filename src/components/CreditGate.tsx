"use client";
import Link from "next/link";import {useEffect,useState} from "react";
export default function CreditGate(){const [open,setOpen]=useState(false);useEffect(()=>{const h=()=>setOpen(true);window.addEventListener("resumeefy:credits-needed",h);return()=>window.removeEventListener("resumeefy:credits-needed",h)},[]);if(!open)return null;return <div className="fixed inset-0 z-[100] bg-black/50 backdrop-blur-sm flex items-center justify-center p-5" role="dialog" aria-modal="true"><div className="card p-7 max-w-md w-full shadow-2xl"><div className="text-xs font-bold text-[var(--blue)]">CREDITS REQUIRED</div><h2 className="font-display text-2xl font-semibold mt-2">You are out of credits.</h2><p className="text-sm text-[var(--ink-soft)] mt-2">Your free credits have done their job. Buy a pack to continue using Resumeefy features.</p><div className="flex gap-2 mt-5"><Link href="/shop" className="btn btn-primary flex-1 justify-center" onClick={()=>setOpen(false)}>Buy credits</Link><button className="btn btn-ghost" onClick={()=>setOpen(false)}>Not now</button></div></div></div>}
